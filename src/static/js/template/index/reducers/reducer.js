import { defaultData } from '../config/config';
import { } from '../actions/action.js';

const reducer = (state = {
}, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default reducer;
